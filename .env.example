POSTGRES_USER=tasks
POSTGRES_PASSWORD=tasks
POSTGRES_DB=tasks

# Host is "db" inside Docker Compose. On the host machine use localhost instead:
# DATABASE_URL=postgresql://tasks:tasks@localhost:5432/tasks
DATABASE_URL=postgresql://tasks:tasks@db:5432/tasks

